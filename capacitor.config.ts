import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mattypattysapps.ironcitadelquest',
  appName: 'Iron Citadel Quest',
  webDir: 'dist'
};

export default config;
