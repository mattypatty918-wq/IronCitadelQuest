import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mattypattysapps.nexusdefense2',
  appName: 'Nexus Defense 2',
  webDir: 'dist'
};

export default config;
