# Codemagic CI/CD for iron-citadel-quest
workflows:
  ios-workflow:
    name: iOS
    max_build_duration: 60
    environment:
      vars:
        XCODE_VERSION: "16.0"
        APP_NAME: "iron-citadel-quest"
    triggering:
      branch_patterns:
        - pattern: master
          include: true
    scripts:
      - git clone https://github.com/mattypatty918-wq/tower-defense-102.git --depth 1
      - cd tower-defense-102 && git submodule update --init --recursive
      - cd tower-defense-102 && xcode-project fetch-dependencies --use-cache
      - cd tower-defense-102 && xcode-project build --workspace  --scheme  --configuration Release
    artifacts:
      - tower-defense-102/ios/*.ipa
    publishing:
      app_store_connect:
        api_key: Whitefoxes
